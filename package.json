{
  "name": "ain-adk-providers",
  "version": "0.1.0",
  "scripts": {
		"build": "npm run build:esm && npm run build:cjs",
		"lint": "npx @biomejs/biome check",
		"lint:write": "npx @biomejs/biome check --write",
    "clean": "yarn clear-build-cache && yarn clear-build-info && lerna clean",
		"prepare": "husky"
	},
  "devDependencies": {
    "lerna": "^8.2.3"
  },
  "license": "MIT"
}
